<div class="container">
    <div class="col s12">
        <h3>Bem vindo ao modulo de afiliados</h3>
        <% if admin_signed_in? %>
            <p>
                Cadastro efetuado com sucesso!
            </p>
            <%= link_to 'Retornar ao módulo administrativo', managers_path %>
        <% end %>
    </div>
</div>